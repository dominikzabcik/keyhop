# Copy to .dev.vars for `npm run dev`. Never commit .dev.vars.
# A GitHub OAuth app with the callback URL http://localhost:8787/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Enables /auth/dev?login=alice on localhost, to try Switchr cloud without GitHub.
DEV_LOGIN=true
